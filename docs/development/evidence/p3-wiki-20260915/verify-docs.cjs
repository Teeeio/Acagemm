const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto'),assert=require('node:assert/strict');const files=["README.md","docs/development/README.md","docs/development/GENERIC_OPERATOR_GOAL.md","docs/development/CURRENT_TASK_HANDOFF.md","docs/development/TEAM_HANDOFF.md","docs/development/REAL_GPU_REGRESSION.md","docs/development/PHASE3_WIKI_ACCEPTANCE.md"];const team=fs.readFileSync(files[4],'utf8'),a=team.indexOf('## 6. 当前设计权威'),b=team.indexOf('## 7. 实施状态',a);assert(a>=0&&b>a);assert.equal(crypto.createHash('sha256').update(team.slice(a,b)).digest('hex'),'7d2e5b71cb40fa5c8515efc45322246af5f66c3c64cf26167fe9124e5778c3b4');let links=0;for(const file of files){const text=fs.readFileSync(file,'utf8');assert(text.slice(0,5000).includes('p3-wiki-20260915/status.json'),file+' missing current evidence');const part=file.endsWith('PHASE3_WIKI_ACCEPTANCE.md')?text:text.slice(0,2500);for(const m of part.matchAll(/\]\(([^\s)]+)\)/g)){const url=m[1].replace(/^<|>$/g,'');if(/^(?:https?:|#|app:)/.test(url))continue;const rel=decodeURIComponent(url.split('#')[0]);if(!rel)continue;assert(fs.existsSync(path.resolve(path.dirname(file),rel)),file+' broken link '+rel);links++;}}console.log(JSON.stringify({files:files.length,links,expertSection6Unchanged:true}));
