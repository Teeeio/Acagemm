# MLA Kernels Demo Workspace

This isolated sample workspace is used by the exhibition build. Applying Candidate 02 writes the reviewed patch into `runtime/mla-kernels` without modifying a user's repository.
