# Baseline Source Service Contract

Selects authoritative, researched, or semantic-fallback baseline sources according to fixed-profile and strict-zero-source policy. Input `{ state, mission, reason }`; output `{ source, semanticFallback }`. Pure strategy only; no filesystem, persistence, or HTTP effects.
