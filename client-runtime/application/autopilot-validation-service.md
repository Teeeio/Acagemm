# Autopilot Validation Service Contract

Starts the candidate Operator Test after Patch application and baseline completion. Matrix resolution is delegated to the injected mission-matrix policy; command execution remains journaled and version-checked. Input is `{ state, mission }`; output is the resulting state.
