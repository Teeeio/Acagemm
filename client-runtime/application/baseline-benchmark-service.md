# Baseline Benchmark Service Contract

Starts the baseline Operator Test with the resolved source, matrix, strict-source flag, and fixed-profile materializer result. Command execution remains journaled and version-checked. Input is `{ state, mission, baselineSource, matrix, strictZeroSource, fixedOperator }`; output is updated state.
