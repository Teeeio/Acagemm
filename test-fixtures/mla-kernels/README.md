# MLA Kernels Test Fixture

This isolated sample workspace is used only by deterministic Reference Fixture tests. Applying Candidate 02 writes the reviewed patch into `runtime/mla-kernels` without modifying a user's repository. It is not a production data source or publishable hardware evidence.
