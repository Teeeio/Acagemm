# Agent 驱动的异构算子优化云：一步到位原型开发文档

> 工作名称：Operator Studio
>
> 文档状态：Prototype Specification v2
>
> 目标读者：产品、交互、前端、客户端、后端、Agent 平台、基础设施和算子工程团队

## 1. 文档结论

本产品不是一个“远程测试队列网页”，而是一套由 Agent 驱动的异构算子优化工作系统。用户通过桌面客户端连接本地代码仓库，以自然语言或结构化目标发起优化任务；云端 Agent 负责理解项目、检索经验、选择 Skill 和 Tool、规划候选、调度异构测试、分析证据、沉淀新经验，并把关键决策交还给用户。

本文所称“OpenDesign 式”参照以本项目给出的产品预期为准：用户面向一个持续工作的智能工作台表达目标，系统内部由多个专业 Agent、Skill 和 Tool 协作完成任务，界面重点呈现过程产物、状态和可控决策，而不是暴露内部编排细节。本文不依赖或假定 OpenDesign 的具体私有实现。

产品由四个不可拆分的核心部分组成：

1. **Operator Studio Client**：用户管理代码、Agent 会话、候选、测试和优化状态的桌面工作台。
2. **Agent Control Plane**：负责长期任务编排、上下文管理、角色协作、权限控制和人工审批。
3. **Knowledge & Capability Cloud**：统一存储、版本化、检索和分发 Experience、Skill、Tool 与 Playbook。
4. **Heterogeneous Execution Cloud**：统一管理 C500、CUDA、ROCm、Ascend、CPU 等环境的测试队列、Worker、证据和产物。

完整产品闭环如下：

```text
用户目标 + 本地代码
        |
        v
桌面客户端创建 Optimization Mission
        |
        v
Agent 检索相关经验 / Skill / Tool / 历史实验
        |
        v
Agent 生成计划与 Candidate Plan，用户按策略审批
        |
        v
本地或云端生成候选代码
        |
        v
云端测试队列路由到指定异构环境
        |
        v
正确性 -> Probe -> Profile -> Full Benchmark -> OJ/外部验证
        |
        v
Evidence Engine 形成可审查结论
        |
        +------> 接受 / 拒绝 / 保留候选 / 请求更多证据
        |
        v
Experience Curator 将结果提炼为可复用云端经验
        |
        v
后续用户和 Agent 可检索、引用和复用
```

## 2. 已确认的产品偏好

- 产品内部以 Agent 驱动，而不是让用户手工串联脚本和页面。
- 桌面客户端是主要入口，Web 管理台是补充入口。
- 云端是任务、能力包、经验、结果和决策记录的唯一可信来源。
- 用户可以连接本地仓库，在客户端中完成算子迭代的绝大多数工作。
- Experience、Skill、Tool 和 Playbook 必须沉淀到云端，并能被其他授权用户复用。
- 检索结果必须高相关、可解释、带来源和证据，不能只是向量相似度列表。
- 测试队列必须在云端统一管理，并支持按任务指定的环境进行可靠路由。
- 不同硬件、驱动、编译器和测试配置必须被建模为明确的环境指纹。
- Worker 只执行测试，不拥有接受候选或更新 current best 的权限。
- Probe、Profile、Full Benchmark 和远端 OJ 属于不同证据等级，不能混用。
- 状态面板首先回答“目标、当前动作、证据、风险和下一步”，原始日志是下钻内容。
- 原型要具备最终产品的架构形态；可以减少平台数量和并发规模，但不使用一次性数据结构。

## 3. 产品目标与边界

### 3.1 产品目标

1. 用户可在客户端连接任意算子仓库并创建优化任务。
2. Agent 可持续运行数小时或数天，任务中断后可以恢复。
3. Agent 每次行动都能说明使用了什么经验、Skill、Tool，以及为什么使用。
4. 用户可把测试矩阵提交到多个云端环境，并实时查看队列和结果。
5. 系统可将分散的运行结果转化为结构化证据和候选决策。
6. 每次成功、失败和诊断结论都可以形成可检索经验。
7. Skill 和 Tool 可在云端发布、版本化、授权、审计和回滚。
8. 所有 Agent 行为、代码修改、命令、结果和决策均可追溯。
9. 一个用户沉淀的通用经验，可以被其他授权用户的 Agent 自动发现和复用。
10. 平台可逐步接入新的硬件环境，而不修改核心任务模型。

### 3.2 原型必须覆盖

- 桌面客户端。
- Agent Mission 工作流。
- 云端 Experience / Skill / Tool 管理与检索。
- 两类以上异构 Worker 的注册、路由和执行。
- Correctness、Benchmark、Profile 三种任务类型。
- 候选对比、证据分级和 adoption 审批。
- 经验自动草拟、人工确认、发布和再次检索。
- 组织、项目和用户权限边界。

### 3.3 原型暂不覆盖

- 公有云商业计费和支付。
- 大规模 Kubernetes 自动扩缩容。
- 完全无人值守地把候选直接合入生产分支。
- 复杂的模型训练平台或通用 CI/CD 平台。
- 所有硬件平台的正式适配；原型只需证明 Adapter 机制。

## 4. 用户与核心使用场景

### 4.1 用户角色

| 角色 | 主要任务 | 核心权限 |
| --- | --- | --- |
| 算子工程师 | 发起优化、审查候选、运行测试 | 项目读写、提交任务、审批候选 |
| 平台工程师 | 接入环境、维护 Worker 和 Runner | 环境管理、Worker 凭证、Runner 发布 |
| 经验维护者 | 审核和发布 Experience / Skill | 能力发布、版本管理、废弃旧版本 |
| 团队负责人 | 查看进度、资源和决策 | 项目概览、审批策略、资源配额 |
| 平台管理员 | 管理组织、安全和全局能力 | 全局管理、审计、策略配置 |

### 4.2 关键场景

**场景 A：从本地仓库开始优化**

用户在客户端选择仓库，系统识别构建方式、算子文件、测试入口和已有优化记录。用户输入“优化 MLA Paged KV Cache 在 C500 上的小 batch 延迟”，Agent 创建 Mission，展示已检索到的经验和准备执行的计划。

**场景 B：跨平台验证**

用户选择 C500、CUDA A100 和 ROCm MI300 环境组成测试矩阵。服务端生成三个子任务，按环境能力路由并聚合结果，客户端展示每个平台的正确性、性能和可比性。

**场景 C：复用团队经验**

Agent 在规划候选前检索到一条“短序列下 plan/cache 固定开销主导”的已验证经验，并同时加载对应 profiling Skill 和 Tool。用户可以看到引用来源、适用条件和历史证据。

**场景 D：沉淀失败经验**

某候选 Correctness 失败。Agent 将失败签名、影响桶、变更变量和禁止重试条件提炼为草稿；经验维护者确认后发布。之后其他项目遇到相同特征时，检索器会优先返回该经验并阻止无条件重试。

## 5. 产品总架构

```text
┌──────────────────────────────────────────────────────────────────────┐
│                         Operator Studio Client                       │
│ Mission / Code Diff / Agent Chat / Candidate Tree / Queue / Evidence│
│ Local Bridge: Git / Files / Terminal / Local Tools / Credential     │
└───────────────────────────────┬──────────────────────────────────────┘
                                │ HTTPS / WebSocket / gRPC stream
┌───────────────────────────────▼──────────────────────────────────────┐
│                         Cloud API Gateway                            │
│ Auth / Organization / Project / Mission / Artifact / Audit          │
└──────────────┬──────────────────────┬──────────────────────┬─────────┘
               │                      │                      │
┌──────────────▼────────────┐ ┌───────▼────────────────┐ ┌──▼───────────────┐
│ Agent Control Plane       │ │ Knowledge & Capability │ │ Execution Cloud  │
│ Durable Workflow          │ │ Experience / Skill     │ │ Scheduler        │
│ Agent Runtime             │ │ Tool / Playbook        │ │ Environment      │
│ Policy & Approval         │ │ Hybrid Retrieval       │ │ Worker / Runner  │
│ Context Pack Builder      │ │ Curator / Evaluation   │ │ Evidence Engine  │
└──────────────┬────────────┘ └───────┬────────────────┘ └──┬───────────────┘
               │                      │                      │
               └──────────────┬───────┴───────────────┬──────┘
                              │                       │
                     ┌────────▼────────┐     ┌────────▼────────┐
                     │ PostgreSQL      │     │ Object Storage  │
                     │ domain + pgvector│    │ source/log/result│
                     └─────────────────┘     └─────────────────┘
                                                        │
                ┌───────────────────────────────────────┼───────────────┐
                ▼                 ▼                     ▼               ▼
          C500 Worker       CUDA Worker          ROCm Worker     Ascend Worker
```

### 5.1 架构原则

- **云端控制，本地协作**：云端保存正式状态，本地客户端保存工作副本和短期缓存。
- **任务与执行解耦**：Mission、Agent Action、Test Task 和 Worker Run 是不同对象。
- **知识与原始记录分离**：原始日志不可变，Experience 是对证据的可维护提炼。
- **能力即版本化资产**：Skill、Tool、Runner Adapter 都有版本、依赖、权限和兼容性声明。
- **证据先于结论**：任何性能或 adoption 结论都必须引用具体 Run 和 Environment Fingerprint。
- **人工审批是策略，不是硬编码页面**：组织可以配置哪些动作需要审批。
- **任务 ID 可观察，数据库字段负责授权**：ID 中包含路由提示，但不能把字符串解析当成安全边界。

## 6. 桌面客户端

### 6.1 客户端定位

客户端不是浏览器页面的简单封装。它承担四类本地能力：

1. 访问本地仓库、分支、文件和未提交变更。
2. 展示并应用 Agent 生成的代码补丁。
3. 调用本地终端和被授权的本地 Tool。
4. 将本地上下文安全同步到云端 Mission。

建议技术栈：

- 桌面容器：Tauri 2。
- UI：React + TypeScript。
- 本地 Bridge：Python sidecar，复用算子生态中的 Python、Git 和构建工具。
- 通信：客户端与 Bridge 使用本机受限端口或 stdio RPC；Bridge 与云端使用 HTTPS/WebSocket。
- 凭证：系统 Keychain，不把长期 Token 写入仓库。

### 6.2 客户端信息架构

```text
工作区
├── Mission 工作台
│   ├── Agent 会话
│   ├── 当前计划
│   ├── 当前动作
│   ├── 审批请求
│   └── 状态与风险
├── Code
│   ├── 文件树
│   ├── Diff 审查
│   ├── Candidate 分支
│   └── 本地终端
├── Experiments
│   ├── Candidate Tree
│   ├── Test Matrix
│   ├── Queue
│   └── Result Comparison
├── Knowledge
│   ├── Experience
│   ├── Skills
│   ├── Tools
│   └── Mission 引用
└── Project Settings
    ├── Repository
    ├── Environments
    ├── Policies
    └── Secrets
```

### 6.3 本地 Bridge 能力边界

允许：

- 读取用户已授权的项目目录。
- 创建候选分支或 worktree。
- 生成和应用 patch，但必须在 UI 中可审查。
- 执行已授权 Tool 的命令。
- 上传源码快照、差异和指定产物。

禁止：

- 未经授权扫描其他目录。
- 静默上传整个用户主目录或无关文件。
- 直接使用云端 Agent 凭证执行任意系统命令。
- 未经审批覆盖用户未提交修改。

### 6.4 离线和断线行为

- 客户端可离线浏览已缓存的 Mission、Diff 和结果。
- 正式 Agent 状态、测试任务和能力版本仍以云端为准。
- 本地操作使用 operation id，重连后幂等同步。
- 离线期间不允许伪造云端任务完成或 adoption 状态。

## 7. Agent Control Plane

### 7.1 Mission 是最高层工作对象

Mission 表示一次持续的优化目标，而不是一次聊天会话。

```json
{
  "mission_id": "mis_01JEXAMPLE",
  "project_id": "prj_mla_20003",
  "title": "优化 MLA Paged KV Cache 在 C500 上的小 batch 延迟",
  "objective": {
    "metric": "latency_ms",
    "direction": "minimize",
    "target_platforms": ["c500-prod", "cuda-a100-reference"],
    "correctness_policy": "all_pass",
    "adoption_policy": "human_approval"
  },
  "phase": "diagnosis",
  "state": "running"
}
```

Mission 必须持久化以下内容：

- 用户目标与成功标准。
- 长、中、短期目标。
- 当前阶段、动作、检查点和下一步。
- 已确认事实、待验证假设和已排除方向。
- 候选树和每个候选的结构化计划。
- 使用过的 Experience、Skill、Tool 及版本。
- 测试任务、结果、证据和决策。
- 所有人工审批、回退和方向变更。

### 7.2 Agent 角色

角色由云端 Agent Profile 定义，不写死在客户端：

| Agent | 职责 | 主要输出 |
| --- | --- | --- |
| Orchestrator | 拆解目标、选择路线、调度角色、维护状态 | Mission Plan、Action、审批请求 |
| Context Agent | 理解仓库、约束、历史和当前最佳 | Context Snapshot |
| Research Agent | 收集外部资料和已有实现 | Research Note |
| Bottleneck Agent | 分析数据流、执行特征和 profile | Bottleneck Report |
| Candidate Agent | 形成候选假设和有界改动 | Candidate Plan、Patch |
| Validation Agent | 生成正确性和性能测试矩阵 | Test Plan |
| Result Analyst | 对比结果、判断证据等级 | Decision Report |
| Experience Curator | 提炼成功、失败和方法论 | Experience Draft |

### 7.3 Agent 行动模型

每个主要 Action 必须包含：

- `what`：要做什么。
- `why`：为什么现在做。
- `how`：使用哪些 Skill、Tool、数据和步骤。
- `expected_output`：预期产物。
- `done_when`：完成条件。
- `serves_goal_id`：服务于哪个目标。
- `risk_level`：风险等级。
- `approval_requirement`：是否需要用户批准。

Action 状态机：

```text
proposed -> awaiting_approval -> running -> completed
                         |            |-> failed
                         |            |-> blocked
                         |            |-> cancelled
                         |-> rejected
```

### 7.4 持久化工作流

Agent 工作流应使用可恢复的 durable workflow。推荐原型采用 Temporal：

- 长时间等待测试结果时不占用进程。
- 支持重试、超时、人工审批和断点恢复。
- 每个 Action 和 Test Task 都有可审计事件。
- Agent 服务重启后可以继续原 Mission。

模型调用不是工作流状态。Prompt、Context Pack、模型版本和响应摘要需要记录，但业务状态必须写入领域数据库。

### 7.5 审批策略

默认策略：

| 动作 | 默认审批 |
| --- | --- |
| 只读检索、分析已有结果 | 自动 |
| 创建 Candidate Plan | 自动，通知用户 |
| 修改候选工作区文件 | 首次需要，之后可按 Mission 授权 |
| 执行本地命令 | 按 Tool 权限和命令风险 |
| 提交云端测试 | 配额内自动 |
| 运行高成本 Full Benchmark | 需要或按预算策略自动 |
| 发布组织级 Skill / Tool | 需要维护者审批 |
| 更新 current best / 合并代码 | 必须满足 gate，并默认人工审批 |

## 8. Knowledge & Capability Cloud

### 8.1 云端资产类型

| 资产 | 作用 | 示例 |
| --- | --- | --- |
| Experience | 有证据的可复用结论 | “短序列下 wrapper 固定开销占主导” |
| Skill | Agent 执行某类工作的流程合同 | “性能瓶颈分段分析” |
| Tool | 可执行能力及参数 Schema | “提交全局测试队列” |
| Playbook | 面向算子或平台的多步骤策略 | “C500 Attention 优化手册” |
| Agent Profile | Agent 角色、工具和策略集合 | “Result Analyst v3” |
| Runner Adapter | 某环境执行测试的实现 | “MXMACA Runner 1.4” |
| Environment Profile | 硬件和软件能力指纹 | “C500 + MXMACA 2.x” |

### 8.2 统一版本模型

Skill、Tool、Playbook、Agent Profile 和 Runner Adapter 都采用不可变版本：

```json
{
  "package_id": "skill.perf-bottleneck-profiling",
  "version": "2.3.1",
  "type": "skill",
  "owner_org_id": "org_platform",
  "visibility": "organization",
  "compatibility": {
    "platforms": ["c500", "cuda", "rocm"],
    "agent_runtime": ">=1.2",
    "required_tools": ["tool.queue.submit@^2"]
  },
  "permissions": ["repository:read", "test_task:create"],
  "content_digest": "sha256:...",
  "signature": "sig:...",
  "status": "published"
}
```

版本一经发布不可覆盖。修订必须产生新版本，Mission 固定引用具体版本以保证复现。

### 8.3 Experience 数据模型

Experience 不是聊天摘要，必须包含适用条件和证据：

```json
{
  "experience_id": "exp_01JEXAMPLE",
  "title": "短序列下优先量化固定开销",
  "summary": "当设备 Kernel 已低于 50us 时，应先分离 plan、workspace、host mirror 和同步开销。",
  "kind": "bottleneck_pattern",
  "scope": {
    "operators": ["attention", "paged_attention"],
    "platforms": ["c500", "cuda"],
    "shape_predicates": ["seq_len <= 1024"]
  },
  "claims": [
    {
      "statement": "固定开销可能成为主要延迟来源",
      "confidence": "high",
      "evidence_run_ids": ["run_001", "run_002"]
    }
  ],
  "recommended_actions": ["skill.perf-bottleneck-profiling@2.3.1"],
  "do_not_retry": [],
  "validation": {
    "status": "validated",
    "validated_at": "2026-08-03T00:00:00Z",
    "reviewer_id": "usr_maintainer"
  },
  "visibility": "organization"
}
```

### 8.4 经验生命周期

```text
observed
   -> draft
   -> validating
   -> validated
   -> published
   -> deprecated
```

- `observed`：由运行、Agent 或用户产生的原始观察。
- `draft`：Curator 形成结构化经验草稿。
- `validating`：等待证据完整性、重复性和适用范围检查。
- `validated`：结论有足够证据，但尚未对更大范围发布。
- `published`：可被目标可见范围内的用户和 Agent 检索。
- `deprecated`：过期、被新经验替代或已不适用。

任何 Experience 都不能删除其证据引用。废弃只改变检索权重和可用状态。

### 8.5 高效检索机制

检索采用五阶段管线：

```text
Mission Context
   -> Query Builder
   -> Hybrid Recall
   -> Evidence-aware Rerank
   -> Policy Filter
   -> Context Pack
```

**Query Builder** 从当前任务生成结构化查询：

- operator / dtype / layout / shape bucket。
- hardware / compiler / driver / runtime。
- 当前阶段：诊断、实现、验证或结果分析。
- 当前假设和已失败方向。
- 需要的资产类型：Experience、Skill、Tool 或 Playbook。

**Hybrid Recall** 同时使用：

- PostgreSQL 结构化过滤。
- PostgreSQL Full Text Search / BM25 风格关键词召回。
- pgvector 语义召回。
- Experience 与 Run、Operator、Platform、Skill 之间的图关系扩展。

**Evidence-aware Rerank** 综合以下信号：

```text
final_score =
  semantic_relevance
  + metadata_match
  + evidence_quality
  + reuse_success
  + freshness
  - contradiction_penalty
  - deprecated_penalty
```

**Policy Filter** 在返回结果前执行组织、项目、用户和资产权限检查，禁止把不可见内容注入 Agent 上下文。

**Context Pack** 只保留完成任务所需的最小内容，并对每条结论附带：

- 资产标题和固定版本。
- 为什么匹配当前任务。
- 适用条件和不适用条件。
- 证据链接。
- 置信度、更新时间和维护者。
- Agent 是否实际采用，以及采用后产生的结果。

### 8.6 检索反馈与质量评估

每次检索记录以下反馈：

- `shown`：是否展示给用户或 Agent。
- `selected`：Agent 是否选择使用。
- `cited`：是否进入计划或决策报告。
- `helpful`：用户或 Agent 是否确认有帮助。
- `outcome`：使用后是成功、失败还是无结论。

平台定期计算：

- Top-k 命中率。
- 被引用率。
- 跨项目复用成功率。
- 过期经验误导率。
- 同类经验冲突率。
- 检索延迟和 Context Pack 大小。

### 8.7 共享与隔离

资产可见性分为：

- `private`：仅创建者。
- `project`：项目成员。
- `organization`：组织内所有授权用户。
- `global`：平台所有授权用户。

通用方法、Skill 和 Tool 可以发布到组织或全局。原始源码、日志和内部路径默认保持项目可见；发布 Experience 时通过 Curator 去除敏感信息。这样既满足全员复用，也避免把项目私有数据直接公开。

## 9. 异构测试云与队列

### 9.1 核心对象关系

```text
Environment Definition
        |
        +-- Environment Instance
        |       +-- Worker Agent
        |       +-- Capability Fingerprint
        |       +-- Runner Adapter Version
        |
Test Plan -> Test Task -> Lease -> Worker Run -> Result -> Evidence
```

### 9.2 Environment Definition

环境不是简单的 `platform=cuda` 标签，而是可验证指纹：

```json
{
  "environment_id": "env_c500_prod_01",
  "display_name": "C500 Production 01",
  "platform": "c500",
  "hardware": {
    "device_model": "Xiyun C500",
    "device_count": 1,
    "memory_bytes": 68719476736
  },
  "software": {
    "os": "linux",
    "compiler": "mxmaca",
    "compiler_version": "2.x",
    "driver_version": "...",
    "container_image_digest": "sha256:..."
  },
  "capabilities": [
    "compile",
    "correctness",
    "benchmark",
    "profile.timeline",
    "profile.counter"
  ],
  "runner_adapter": "runner.mxmaca@1.4.0",
  "queue_policy": {
    "max_concurrency": 1,
    "lease_ttl_seconds": 180,
    "default_timeout_seconds": 900
  }
}
```

每次 Run 保存实际指纹，环境升级后不修改历史 Run。

### 9.3 任务 ID 与路由

任务 ID 使用可读路由提示：

```text
tsk.{environment_short_id}.{ulid}
```

示例：

```text
tsk.c500-prod-01.01JEXAMPLE
tsk.cuda-a100-02.01JEYAMPLE
```

创建任务时必须写入正式字段：

- `target_environment_id`：精确指定环境。
- `target_environment_selector`：按能力选择环境。
- `routing_key`：内部队列键。
- `task_id`：给用户、日志和 Worker 使用的稳定标识。

Scheduler 可以从 task id 快速获得路由提示，但最终路由和授权必须校验数据库字段。客户端输入 task id 时，服务端先解析前缀，再读取正式记录，避免伪造 ID 跨环境执行。

### 9.4 Test Plan 与测试矩阵

用户或 Validation Agent 先创建 Test Plan，再展开为 Test Task：

```json
{
  "test_plan_id": "tpl_01JEXAMPLE",
  "candidate_id": "cand_450",
  "matrix": {
    "environments": ["env_c500_prod_01", "env_cuda_a100_02"],
    "stages": ["correctness", "probe", "full_benchmark"],
    "case_sets": ["short", "long"],
    "compile_profiles": ["release"]
  },
  "gates": [
    "correctness must pass before performance",
    "full_benchmark requires probe threshold or approval"
  ]
}
```

矩阵展开后，阶段之间用依赖关系连接。Correctness 失败时，下游性能任务自动跳过并说明原因。

### 9.5 Test Task 状态机

```text
created -> queued -> leased -> preparing -> running -> uploading -> completed -> reviewed
             |          |          |          |             |
             |          |          |          |             +-> invalid
             |          |          |          +-> timeout
             |          |          +-> failed
             |          +-> lease_expired -> queued
             +-> cancelled
```

任务支持：

- 优先级和组织配额。
- 幂等创建键。
- 最大重试次数和退避策略。
- 依赖任务和 gate 条件。
- 预约时间和 deadline。
- 资源需求与环境能力匹配。
- 用户取消和管理员终止。

### 9.6 Worker 协议

Worker 采用出站连接，测试机无需暴露入站端口：

1. 使用短期 Worker 凭证注册。
2. 上报环境指纹和可用能力。
3. 对自己的 routing key 发起 long poll。
4. 获得任务 lease 和签名下载地址。
5. 在隔离 workspace 中执行 Runner Adapter。
6. 周期性发送 heartbeat 和阶段进度。
7. 上传结构化结果和原始产物。
8. 服务端确认后清理 workspace。

Worker 不允许：

- 修改 Mission 决策。
- 更新 current best。
- 发布 Experience、Skill 或 Tool。
- 执行未在任务清单和 Tool 权限内的命令。

### 9.7 Runner Adapter 契约

```python
class RunnerAdapter:
    def inspect_environment(self) -> EnvironmentFingerprint: ...
    def prepare(self, task: TestTask) -> PreparedWorkspace: ...
    def compile(self, workspace: PreparedWorkspace) -> StageResult: ...
    def validate(self, workspace: PreparedWorkspace) -> StageResult: ...
    def benchmark(self, workspace: PreparedWorkspace) -> StageResult: ...
    def profile(self, workspace: PreparedWorkspace) -> StageResult: ...
    def collect_artifacts(self, workspace: PreparedWorkspace) -> list[Artifact]: ...
    def cleanup(self, workspace: PreparedWorkspace) -> None: ...
```

Adapter 输出遵循统一 Schema，但可以保留平台专属扩展字段。

### 9.8 Artifact 与结果

以下产物必须进入对象存储并生成 digest：

- 源码快照或 patch。
- 构建日志和编译产物摘要。
- correctness CSV/JSON。
- benchmark 原始数据和聚合结果。
- profiler trace、counter 和报告。
- 标准输出、标准错误和环境信息。
- Agent 生成的分析报告。

Result 是结构化索引，Artifact 是不可变原件。UI 默认展示 Result，需要时再打开 Artifact。

## 10. Evidence Engine 与候选决策

### 10.1 证据等级

| 等级 | 证据 | 可支持的结论 |
| --- | --- | --- |
| L0 | 假设 / Candidate Plan | 允许实现，不说明正确或更快 |
| L1 | Code Diff / Build Pass | 说明存在且可构建 |
| L2 | Correctness Pass | 允许解释性能 |
| L3 | Probe / Screening | 筛选方向，不允许 adoption |
| L4 | Diagnostic Profile | 判断瓶颈，不允许 adoption |
| L5 | Full Benchmark | adoption 的必要条件之一 |
| L6 | Gate Review | 可提出 adoption 决策 |
| L7 | Approved Adoption | 正式更新 current best |

### 10.2 候选状态

```text
draft
 -> planned
 -> implemented
 -> correctness_passed
 -> screening
 -> weak_candidate | rejected | full_needed
 -> full_verified
 -> awaiting_adoption
 -> adopted | archived
```

### 10.3 对比维度

Evidence Engine 至少生成：

- Candidate 对 Current Best。
- Candidate 对固定 Baseline。
- 各平台内的可比结果。
- Shape bucket 和 case 级变化。
- 总延迟、均值、P50/P95 和吞吐。
- Correctness、数值误差和失败签名。
- 编译时间、运行时内存和稳定性。
- 环境指纹差异和不可比原因。

### 10.4 决策报告

```json
{
  "decision": "full_needed",
  "summary": "短序列 bucket 有 1.8% 稳定收益，未发现严重回退。",
  "evidence_level": "L3",
  "confidence": "medium",
  "wins": ["c500 short batch=1"],
  "regressions": [],
  "missing_evidence": ["full benchmark", "repeatability x3"],
  "recommended_next_action": "submit full benchmark",
  "run_ids": ["run_101", "run_102"]
}
```

所有结论必须区分：已确认事实、待验证假设、已排除方向和用户决策。

## 11. 核心产品页面

### 11.1 首页：Mission Dashboard

回答三个问题：正在优化什么、进展在哪里、需要用户做什么。

```text
┌──────────────────────────────────────────────────────────────────┐
│ Operator Studio   Project: MLA Paged KV Cache      Env: 3 online │
├──────────────────────────────────────────────────────────────────┤
│ Mission: 优化 C500 小 batch 延迟          [Running] [Pause]      │
│ Goal: 全量正确，Current Best 至少提升 1%                          │
│ Phase: Diagnosis > Candidate > Validation > Adoption              │
├───────────────────────┬──────────────────────────────────────────┤
│ Current Action        │ Evidence Summary                         │
│ Profile T450          │ Correctness 20/20 PASS                   │
│ Why: 定位固定开销      │ Probe +0.62% / confidence medium         │
│ Tool: mcTracer v2     │ Missing: full benchmark                  │
│ ETA / current stage   │ [Open evidence]                          │
├───────────────────────┼──────────────────────────────────────────┤
│ Candidate Tree        │ Queue                                    │
│ T97 current best      │ C500   running   3/6                     │
│ └─ T450 weak          │ A100   queued    position 2              │
│    └─ T454 planned    │ MI300  completed PASS                    │
├───────────────────────┴──────────────────────────────────────────┤
│ Attention: 1 approval required                                   │
│ Run full benchmark?  Estimated cost 18 min   [Approve] [Reject]  │
└──────────────────────────────────────────────────────────────────┘
```

### 11.2 Mission 工作台

左侧显示 Agent 对话和用户输入；中间显示当前计划、Action 和引用的能力；右侧显示目标、事实、假设、风险和审批。用户不需要阅读日志就能知道 Agent 为什么做当前动作。

### 11.3 Code 与 Diff

- 文件树和代码编辑器。
- Candidate 与 parent 的 diff。
- 每个变更块显示 Agent 的目的和关联假设。
- 用户可接受单个 hunk、整个 patch 或拒绝。
- 显示工作区脏状态，禁止静默覆盖。
- 支持打开本地终端和复制复现命令。

### 11.4 Experiment Board

按 Candidate 展示测试阶段：

```text
Candidate T454
[Build PASS] -> [Correctness PASS] -> [Probe RUNNING] -> [Full LOCKED]
```

支持平台、shape bucket、证据等级和状态筛选。候选卡只展示摘要，不嵌套大段日志。

### 11.5 Queue & Environments

- 环境在线状态和最后心跳。
- 当前运行任务、队列长度和预计等待时间。
- 环境指纹变更提醒。
- 任务优先级、配额和取消入口。
- 通过 task id 搜索并看到完整路由链路。
- 管理员可将环境设为 draining，不再接受新任务。

### 11.6 Result Comparison

默认视图是决策表：

| Bucket | Current Best | Candidate | Delta | Correctness | Confidence |
| --- | ---: | ---: | ---: | --- | --- |
| B1 / S1024 / H64 | 0.039 ms | 0.038 ms | -2.56% | PASS | high |
| B16 / S16384 / H128 | 0.704 ms | 0.718 ms | +1.99% | PASS | medium |

用户可下钻到重复样本、原始日志、profile trace 和环境差异。

### 11.7 Knowledge Explorer

资产卡显示：

- 标题、类型、版本和维护者。
- 匹配当前 Mission 的原因。
- 适用平台、算子和 shape 条件。
- 证据质量和复用成功率。
- 被哪些 Agent 和 Mission 使用过。
- 安装、固定版本、查看差异和废弃操作。

### 11.8 Status 面板

Status 面板固定使用以下层级：

1. **目标与当前阶段**：长期目标、当前 checkpoint、是否阻塞。
2. **当前动作**：what、why、how、预期输出和完成条件。
3. **已确认成果**：只记录真正推进任务的结果。
4. **知识状态**：事实、假设、已排除方向。
5. **下一步、风险与用户注意**：明确谁需要做什么。
6. **事件时间线**：折叠展示，原始日志不占主页面。

进度由 checkpoint 计算，禁止使用主观百分比。

## 12. 关键数据模型

### 12.1 主要表

| 表 | 作用 |
| --- | --- |
| `organizations` / `users` / `memberships` | 多租户与权限 |
| `projects` / `repositories` | 项目和仓库连接 |
| `missions` / `goals` / `actions` / `checkpoints` | Agent 工作状态 |
| `agent_profiles` / `agent_runs` / `model_calls` | Agent 配置与调用审计 |
| `candidates` / `candidate_plans` / `patches` | 候选与代码变更 |
| `experiences` / `experience_claims` / `experience_links` | 经验和证据图 |
| `packages` / `package_versions` / `package_permissions` | Skill、Tool 等能力包 |
| `environments` / `environment_snapshots` / `workers` | 执行环境和 Worker |
| `test_plans` / `test_tasks` / `task_dependencies` / `leases` | 测试队列 |
| `runs` / `results` / `metrics` / `artifacts` | 执行结果和原始产物 |
| `evidence_reports` / `decisions` / `approvals` | 证据与决策 |
| `retrieval_queries` / `retrieval_hits` / `retrieval_feedback` | 检索质量闭环 |
| `audit_events` | 不可变审计事件 |

### 12.2 关键约束

- `candidate.parent_candidate_id` 构成候选树，禁止循环。
- `run.environment_snapshot_id` 固定历史环境，不能引用可变当前值。
- `test_task.idempotency_key` 在项目内唯一。
- `package_version.content_digest` 唯一且发布后不可变。
- `experience_claim.evidence_link` 至少引用一个可访问证据或明确标记为假设。
- `decision` 必须包含 evidence level 和引用的 run ids。
- `adoption` 必须引用审批和 gate report。
- 删除项目时先进入 retention 状态，不直接删除审计和合规记录。

## 13. API 草案

### 13.1 客户端与项目

```text
POST   /v1/projects
POST   /v1/projects/{id}/repository-sessions
GET    /v1/projects/{id}/snapshot
POST   /v1/artifacts/upload-sessions
GET    /v1/artifacts/{id}
```

### 13.2 Mission 与 Agent

```text
POST   /v1/missions
GET    /v1/missions/{id}
POST   /v1/missions/{id}/messages
POST   /v1/missions/{id}/pause
POST   /v1/missions/{id}/resume
GET    /v1/missions/{id}/events
GET    /v1/missions/{id}/status
POST   /v1/actions/{id}/approve
POST   /v1/actions/{id}/reject
```

### 13.3 Candidate 与证据

```text
POST   /v1/missions/{id}/candidates
GET    /v1/candidates/{id}
GET    /v1/candidates/{id}/diff
POST   /v1/candidates/{id}/apply
GET    /v1/candidates/{id}/evidence
POST   /v1/candidates/{id}/adoption-proposals
POST   /v1/adoption-proposals/{id}/approve
```

### 13.4 Experience / Skill / Tool

```text
POST   /v1/knowledge/search
POST   /v1/experiences
POST   /v1/experiences/{id}/validate
POST   /v1/experiences/{id}/publish
GET    /v1/packages
POST   /v1/packages
POST   /v1/packages/{id}/versions
POST   /v1/package-versions/{id}/publish
POST   /v1/projects/{id}/package-locks
```

检索请求示例：

```json
{
  "project_id": "prj_mla_20003",
  "mission_id": "mis_01JEXAMPLE",
  "query": "small batch latency bottleneck",
  "filters": {
    "asset_types": ["experience", "skill", "tool"],
    "platforms": ["c500"],
    "operators": ["paged_attention"],
    "minimum_validation": "validated"
  },
  "limit": 12
}
```

### 13.5 环境、队列和 Worker

```text
POST   /v1/environments
GET    /v1/environments
POST   /v1/workers/register
POST   /v1/workers/{id}/heartbeat
POST   /v1/workers/{id}/lease
POST   /v1/test-plans
GET    /v1/test-tasks/{task_id}
POST   /v1/test-tasks/{task_id}/cancel
POST   /v1/test-tasks/{task_id}/progress
POST   /v1/test-tasks/{task_id}/results
POST   /v1/test-tasks/{task_id}/complete
```

所有写接口接受 `Idempotency-Key`，所有对象包含 `organization_id`，所有跨组织读取先经过权限过滤。

## 14. 实时事件协议

客户端通过 WebSocket 订阅 Mission：

```json
{
  "event_id": "evt_01JEXAMPLE",
  "mission_id": "mis_01JEXAMPLE",
  "sequence": 418,
  "type": "test_task.progress",
  "timestamp": "2026-08-03T12:00:00Z",
  "payload": {
    "task_id": "tsk.c500-prod-01.01JEXAMPLE",
    "stage": "benchmark",
    "completed_cases": 12,
    "total_cases": 24
  }
}
```

客户端按 `sequence` 检测丢失事件，断线重连后通过 `after_sequence` 补拉。事件只用于实时体验，正式状态仍从 REST API 和数据库快照读取。

## 15. 安全与治理

### 15.1 身份和权限

- 用户：OIDC / SSO。
- 客户端：用户授权的设备会话和短期访问 Token。
- Worker：环境专属机器身份和可轮换凭证。
- Agent：服务身份，只能使用 Mission 授予的能力。
- Tool：声明权限，执行前由 Policy Engine 检查。

### 15.2 Tool 安全

- Tool 包必须签名并固定 digest。
- Tool 声明文件、网络、命令、Secret 和测试权限。
- 高风险 Tool 在 sandbox 或受控 Worker 中运行。
- Secret 通过 Vault 临时注入，不进入 Prompt、日志或 Artifact。
- Agent 不能通过自然语言绕过 Tool 参数 Schema 和策略检查。

### 15.3 代码与产物安全

- 上传范围由客户端明确显示。
- 源码、patch 和 artifact 全部计算 digest。
- 下载使用短期签名 URL。
- Worker workspace 按任务隔离，任务结束后清理。
- Artifact 访问继承项目权限。
- 经验发布前执行敏感路径、凭证和源码片段检查。

### 15.4 审计

审计事件至少覆盖：

- 登录和设备授权。
- Agent Action 和所用能力版本。
- 用户批准、拒绝和策略变更。
- Tool 执行和测试任务提交。
- Artifact 上传下载。
- Experience、Skill、Tool 的发布和废弃。
- Candidate adoption 和 current best 变更。

## 16. 推荐技术栈

### 16.1 客户端

- Tauri 2。
- React + TypeScript。
- TanStack Query + Zustand。
- Monaco Editor。
- xterm.js。
- Python Local Bridge。

### 16.2 云端

- API：FastAPI。
- Agent Runtime：Python。
- Durable Workflow：Temporal。
- 领域数据库：PostgreSQL。
- 语义检索：pgvector。
- 关键词检索：PostgreSQL FTS；数据量扩大后再引入 OpenSearch。
- Artifact：S3 / MinIO。
- 测试队列：PostgreSQL authoritative state + Scheduler long poll；需要更高吞吐时增加 Redis Streams。
- 实时事件：WebSocket + Redis Pub/Sub 或 NATS。
- Secret：Vault 或云 KMS。
- 可观测性：OpenTelemetry + Prometheus + Grafana + Loki。

### 16.3 为什么采用该组合

- Python 与当前算子、Agent 和测试生态一致。
- Temporal 适合长时间 Agent 任务、人工审批和可恢复等待。
- PostgreSQL 同时承载领域数据、全文检索和初期向量检索，减少原型基础设施数量。
- 专用 Scheduler 保留异构环境路由、配额和证据 gate 的业务控制能力。
- Tauri 可以提供本地文件和终端能力，同时复用 Web 前端技术。

## 17. Monorepo 建议

```text
operator-studio/
├── apps/
│   ├── desktop/                 # Tauri + React
│   └── web-console/             # 管理和只读访问
├── services/
│   ├── api-gateway/
│   ├── agent-control-plane/
│   ├── knowledge-service/
│   ├── execution-scheduler/
│   ├── evidence-engine/
│   └── realtime-gateway/
├── workers/
│   ├── worker-agent/
│   └── runner-adapters/
│       ├── generic-shell/
│       ├── mxmaca/
│       └── cuda/
├── packages/
│   ├── contracts/               # OpenAPI / JSON Schema / protobuf
│   ├── ui/
│   ├── client-sdk/
│   ├── agent-sdk/
│   └── tool-sdk/
├── deploy/
│   ├── docker-compose/
│   └── helm/
└── docs/
    ├── architecture/
    ├── api/
    └── prototype/
```

原型可作为模块化单体部署，但代码边界按上述服务领域划分，避免后续拆分时重写模型。

## 18. 原型实施范围

### 18.1 一步到位的垂直闭环

原型演示必须完整跑通：

1. 用户安装并登录桌面客户端。
2. 连接一个本地 Git 仓库。
3. 创建 Optimization Mission。
4. Agent 读取项目上下文并检索云端 Experience、Skill 和 Tool。
5. UI 展示检索引用和 Agent 计划。
6. Agent 创建 Candidate Plan 和代码 patch。
7. 用户在 Diff 页面批准并应用 patch。
8. Validation Agent 创建 C500 + CUDA 测试矩阵。
9. 云端 Scheduler 将任务路由给两个 Worker。
10. Worker 完成 Correctness 和 Benchmark，上传结构化结果。
11. Evidence Engine 形成 Candidate 对 Current Best 的报告。
12. 用户批准或拒绝候选。
13. Curator 生成 Experience 草稿，维护者发布。
14. 第二个用户或新 Mission 能检索到该 Experience。

少任何一步，都只能算局部技术 Demo，不算本产品原型完成。

### 18.2 原型环境数量

- 1 个 Generic Shell / CPU Worker，用于开发和自动化测试。
- 1 个真实 C500 Worker。
- 1 个 CUDA Worker；若硬件暂不可用，可先使用第二个独立环境模拟路由，但正式验收前需真实异构环境。

### 18.3 原型内置能力

- 3 个 Agent Profile：Orchestrator、Result Analyst、Experience Curator。
- 3 个 Skill：上下文快照、结果分析、经验沉淀。
- 4 个 Tool：读取仓库、应用 patch、提交测试、读取结果。
- 2 个 Runner Adapter：Generic Shell 和至少一个真实加速器平台。
- 10 条高质量种子 Experience，用于验证检索质量。

## 19. 开发阶段

### Phase 0：契约与基础设施，1 周

- 完成 OpenAPI、事件、Package Manifest 和 Runner Adapter Schema。
- 建立 PostgreSQL、对象存储、Temporal 和认证。
- 搭建 monorepo、CI 和本地 Docker Compose。

完成条件：客户端、服务端和 Worker 能基于共享契约生成 SDK 并通过契约测试。

### Phase 1：客户端与 Mission 主链路，2 周

- 桌面登录、项目连接和 Local Bridge。
- Mission 创建、Agent 会话、状态面板和实时事件。
- Diff 审查和 patch 应用。

完成条件：用户可以在客户端创建持久化 Mission，并安全应用一个 Agent patch。

### Phase 2：异构测试云，2 周

- 环境注册、Worker、lease、heartbeat 和 Scheduler。
- Test Plan、测试矩阵、Correctness 与 Benchmark。
- Artifact 上传和 Result Comparison。

完成条件：同一候选可被路由到两个环境并回传可比较结果。

### Phase 3：能力云与检索，2 周

- Experience、Skill、Tool 包管理。
- 混合检索、Context Pack 和引用展示。
- Curator 草稿、验证、发布和反馈记录。

完成条件：新发布经验能被另一个 Mission 检索并引用。

### Phase 4：Evidence、审批与产品打磨，1 至 2 周

- 候选树、证据等级、Decision Report 和 adoption proposal。
- 权限、审计、Secret、失败恢复和队列管理。
- 完整原型演示、性能和可靠性验收。

完成条件：完整垂直闭环通过，关键失败路径可恢复。

## 20. 验收标准

### 20.1 产品验收

- 用户首次使用不需要手工配置服务端 URL 以外的内部参数。
- 从创建 Mission 到首次任务提交不超过 5 个主要操作。
- 首页无需展开日志即可看懂目标、阶段、证据、阻塞和下一步。
- 每个 Agent 结论都能追踪到 Experience、Skill、Tool 或 Run。
- 用户能在一个页面比较多个平台和多个候选。

### 20.2 Agent 验收

- Mission 服务重启后可恢复。
- Action 包含 what、why、how、输出和完成条件。
- Agent 不会把 Probe 当成 adoption 证据。
- 高风险动作能正确暂停并等待审批。
- Agent 使用的能力版本和 Context Pack 可回放。

### 20.3 检索验收

- 结构化过滤、关键词和向量召回同时生效。
- 不可见资产不会出现在返回结果或模型上下文。
- Top 5 结果展示匹配理由和证据。
- 已废弃或冲突经验有明显警告并降低排序。
- 新发布经验在 60 秒内可检索。

### 20.4 队列验收

- 任务按 `target_environment_id` 正确路由。
- Worker 断线后 lease 可回收并按策略重试。
- 相同幂等键不会创建重复执行。
- Correctness 失败会阻止下游性能任务。
- 每个 Run 固定环境指纹和 Runner Adapter 版本。
- 客户端可通过 task id 查询路由、进度、结果和失败原因。

### 20.5 安全验收

- 项目成员无法读取其他组织的源码、Artifact 和 Experience。
- Worker 凭证不能调用用户或管理 API。
- Tool 无法获得未声明权限。
- Secret 不出现在 Prompt、日志和 Artifact 中。
- 所有发布、审批和 adoption 行为都有审计事件。

### 20.6 原型性能目标

- 普通 API P95 小于 300 ms。
- WebSocket 状态事件端到端延迟小于 1 秒。
- Knowledge Search P95 小于 1.5 秒。
- Worker 空闲时接收任务延迟小于 3 秒。
- 10 万条 Experience 规模下 Top-k 检索仍满足上述目标。

## 21. 不允许被简化掉的设计

开发原型时，以下内容不能用临时字段或 UI 假数据替代：

- Mission、Action、Candidate、Test Task、Run、Evidence 和 Experience 的独立实体。
- 云端正式状态与客户端本地缓存的边界。
- Skill / Tool 的固定版本和权限声明。
- Environment Snapshot 与历史 Run 的绑定。
- Experience 的证据引用和可见性过滤。
- Correctness 到 Benchmark 的 gate。
- Worker 无 adoption 权限。
- 用户审批和审计链路。

可以简化的是部署规模、Worker 数量、Agent 角色数量和检索数据量；不能简化核心领域模型。

## 22. 最终原型演示脚本

演示以一个真实算子仓库为例：

1. 用户在桌面客户端打开仓库，项目页显示 Git 状态和已连接环境。
2. 用户输入优化目标，Mission Dashboard 立即显示目标链和当前动作。
3. Agent 展示三条相关 Experience、一个 Skill 和两个 Tool，并说明匹配原因。
4. Agent 生成 Candidate Plan 和 patch，用户在 Diff 页面批准。
5. 测试矩阵同时提交 C500 和 CUDA；Queue 页面显示两个不同 task id 和路由。
6. C500 Worker 运行，CUDA Worker 暂时离线；系统显示等待和 lease 状态，而不是卡死。
7. CUDA Worker 上线后自动领取任务，两边结果回传。
8. Result Comparison 显示 Correctness、性能变化、环境差异和证据等级。
9. Agent 给出“需要 Full Benchmark”而不是错误地直接接受候选。
10. Full Benchmark 完成后，用户批准 adoption proposal。
11. Curator 自动生成一条带证据和适用条件的 Experience 草稿。
12. 维护者发布后，新建 Mission 能在检索结果中看到并引用该经验。

当这套演示可以稳定重复执行时，原型已经证明：客户端体验、Agent 控制、能力共享、异构执行和经验闭环属于同一个可产品化系统。
